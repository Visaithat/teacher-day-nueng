/** The cassette transport's own measurements. */

/**
 * The transport under the cassette.
 *
 * Furniture, and outside the named hero on purpose: the tape crossed from the
 * envelope and the reader watched it come, but the deck it lands in belongs to
 * the room. It arrives with the pan, in the two hundred milliseconds the camera
 * still has to run once the cassette has already settled.
 */
export const TAPE = {
  /**
   * Marks in the row.
   *
   * Forty and not twenty-odd, and the reason is the shape of one mark rather
   * than the look of the row: a mark is as wide as the room it is given, and
   * at twenty-six across this pill each one came out about as wide as the
   * shortest of them was tall — so the quiet parts of the song were drawn as
   * dots. Thin marks read as sound. Wide ones read as a bar chart.
   */
  bars: 40,
  /** One mark's whole breath. */
  beat: 900,
  /**
   * How far apart two neighbours are in it, so the row ripples rather than
   * pumps. Spent as a NEGATIVE delay, so every mark starts partway through its
   * own breath and the row is alive on the first frame rather than rising into
   * life together.
   */
  stagger: 42,
};
